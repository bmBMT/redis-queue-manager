{
  "name": "@redis-queue-manager/prisma",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && prisma generate",
    "generate": "prisma generate",
    "migrate": "dotenv -e ../../environments/prisma.env -- prisma migrate dev",
    "reset": "dotenv -e ../../environments/prisma.env -- prisma migrate reset",
    "deploy": "dotenv -e ../../environments/prisma.env -- npx prisma migrate deploy"
  },
  "main": "dist/index.js",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.15.0"
  },
  "devDependencies": {
    "prisma": "^6.15.0"
  },
  "prisma": {
    "schema": "./src/schema"
  }
}
